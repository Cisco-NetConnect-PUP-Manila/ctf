export default function ScrollGlitch() {
  // Scroll-triggered glitching is intentionally disabled for production readability.
  // The static CRT styling and intentional intro/title glitches remain elsewhere.
  return <div className="glitch-fx" aria-hidden="true" />;
}
